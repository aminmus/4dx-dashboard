/** Type definitions for User Roles
 * @module Utils/roles
 */

/**
 * User roles
 * @type {Object}
 * @property {string} admin - Admin role
 */
const ROLES = {
  admin: 'admin',
};

module.exports = ROLES;
